{% extends 'base.html.twig' %}
{% block body %}

    <div class="card">
        <div class="card-body ">
            <div class="d-flex flex-row">

                <div class="flex-grow-0">
                    <img src="{{ asset('images/teachers/' ~ teacher.image) }}" alt="..."
                         width="200">
                </div>
                <div class="flex-grow-1 p-2">
                    <p>
                        {{ teacher.name }}
                    </p>
                    <p>
                        {{ teacher.description }}
                    </p>
                </div>

            </div>
        </div>
    </div>

{% endblock %}